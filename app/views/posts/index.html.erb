<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Date</th>
      <th>Tag</th>
      <th colspan="4"></th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.created_at.strftime("%Y/%m/%d")%>
        <td><%= render 'shared/tag_list', tag_list: post.tag_list %></td>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
